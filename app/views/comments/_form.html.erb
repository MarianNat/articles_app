<%= form_for [@article, @comment] do |f| %>
  <div class="field">
    <%= f.label :comment, 'Comment' %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="action">
    <%= f.submit 'Send' %>
  </div>
<% end %>